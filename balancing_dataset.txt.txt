# Creating balanced dataset using SMOTE
x_bal, y_bal = smote.fit_resample(x, y)

print(y.value_counts())
print(y_bal.value_counts())

names = x_bal.columns

##Observations:
- The dataset was imbalanced before applying SMOTE.
- SMOTE generated synthetic samples for the minority class.
- After balancing, both classes contain an equal number of samples.
- The balanced dataset helps improve model performance and reduces bias.