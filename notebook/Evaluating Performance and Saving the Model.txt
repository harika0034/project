from sklearn.model_selection import cross_val_score
import pickle  

# Performing 5-fold cross validation
scores = cross_val_score(model, X_train, y_train, cv=5)

print("Cross Validation Scores:", scores)
print("Average Accuracy:", scores.mean())

# Save the trained model
pickle.dump(model, open("rdf.pkl", "wb"))

print("Model saved successfully.")

# Load the saved model
loaded_model = pickle.load(open("rdf.pkl", "rb"))

print("Saved model loaded successfully.")

# Evaluating Performance and Saving the Model

## Observations

- Cross-validation was performed using 5-fold validation.
- The model achieved consistent accuracy across different folds.
- The average cross-validation accuracy indicates the model generalizes well.
- The trained model was saved using the `pickle` library as a `.pkl` file.
- The saved model can be loaded later in the Flask application for prediction without retraining.